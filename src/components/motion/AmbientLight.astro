<span class="ambient-light" aria-hidden="true"></span>

<style>
  .ambient-light {
    position: absolute;
    inset: -45%;
    pointer-events: none;
    background: radial-gradient(
      circle at 46% 42%,
      color-mix(in srgb, var(--color-ivory) 42%, transparent) 0%,
      color-mix(in srgb, var(--color-gold) 22%, transparent) 18%,
      color-mix(in srgb, var(--color-burgundy) 10%, transparent) 38%,
      transparent 68%
    );
    opacity: var(--motion-light-opacity);
    transform: translate3d(-2%, -1%, 0) scale(0.96);
    animation: ambient-light-drift 26s var(--motion-ease-cinematic) infinite
      alternate;
  }

  @keyframes ambient-light-drift {
    to {
      opacity: 0.1;
      transform: translate3d(3%, 2%, 0) scale(1.04);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ambient-light {
      display: none;
    }
  }
</style>
